[project]
name = "trust-layer"
version = "0.1.0"
description = "Credibility verification for AI research output — Claude Impact Lab Munich"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "pydantic>=2.9",
]
