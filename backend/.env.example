# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional — per-stage model override.
# The scorer runs once per source, so it is the one that adds up on a big bibliography.
# MODEL_SURVEY=claude-opus-4-6
# MODEL_EXTRACT=claude-opus-4-6
# MODEL_SCORE=claude-opus-4-6
# MODEL_AGGREGATE=claude-opus-4-6
